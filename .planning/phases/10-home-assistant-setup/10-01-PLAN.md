---
phase: 10-home-assistant-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ha-setup-guide.md
autonomous: true
requirements:
  - HAIG-01
  - HAIG-02
  - HAIG-03

must_haves:
  truths:
    - "Father can follow numbered steps to add template sensors, plant monitors, and alert automations to his HA configuration"
    - "Father can paste dashboard YAML into HA card editor and see per-bed moisture and temperature gauges"
    - "Father knows exactly how to find his notification service name and where to replace the placeholder in automations"
  artifacts:
    - path: "docs/ha-setup-guide.md"
      provides: "Complete HA setup walkthrough with all YAML blocks"
      min_lines: 200
      contains: "notify.mobile_app_REPLACEME"
  key_links:
    - from: "docs/ha-setup-guide.md"
      to: "data/ha/sensors.json"
      via: "YAML blocks extracted from yaml_output fields"
      pattern: "sensor\\.haven_bed_[ab]_moisture"
    - from: "docs/ha-setup-guide.md"
      to: "data/ha/alert-rules.json"
      via: "Automation YAML derived from alert rules"
      pattern: "notify\\.mobile_app_REPLACEME"
---

<objective>
Create a step-by-step Home Assistant setup walkthrough that a father with HA already running can follow to configure garden sensors, plant monitors, alert automations, and a Lovelace dashboard -- using the YAML already prepared in data/ha/*.json files.

Purpose: The father should not need to consult Claude again for HA setup. One document, copy-paste YAML, working garden dashboard.
Output: docs/ha-setup-guide.md
</objective>

<execution_context>
@/Users/albair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-home-assistant-setup/10-RESEARCH.md

@data/ha/sensors.json
@data/ha/plants.json
@data/ha/alert-rules.json
@data/ha/customize.json
@data/sensors/sensor-recommendations.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HA setup walkthrough (Steps 1-6)</name>
  <files>docs/ha-setup-guide.md</files>
  <action>
Create docs/ha-setup-guide.md with the following structure. Extract all YAML blocks from the data/ha/*.json yaml_output fields and the 10-RESEARCH.md code examples -- do NOT invent new YAML, use what exists.

**Document structure (Steps 1-6):**

1. **Prerequisites** -- What must exist before starting: HA running, Zigbee2MQTT installed, Haozee sensors paired and reporting. Frame this as a reference guide for when sensors arrive (sensors are v2 scope, not yet purchased). Include note about the haven_ entity naming convention.

2. **Template Sensors** -- Complete YAML block for configuration.yaml with all 4 template sensors (bed_a_moisture, bed_a_temperature, bed_b_moisture, bed_b_temperature). Source: data/ha/sensors.json yaml_output. Include note about replacing source entity names if Zigbee2MQTT names differ. Add "Check Configuration" step before restart.

3. **Entity Customization** -- YAML block for customize.yaml with friendly names and icons for all 4 sensors. Source: data/ha/customize.json yaml_output. Include the homeassistant: customize: wrapper.

4. **Plant Monitors** -- YAML block for configuration.yaml with haven_bed_a and haven_bed_b plant monitors including moisture/temperature thresholds. Source: data/ha/plants.json yaml_output. Note that plant monitors will show "problem" until real sensor data flows -- this is expected.

5. **Alert Automations** -- All 10 automation rules from data/ha/alert-rules.json converted to HA automation YAML using the modern `action:` keyword (not deprecated `service:`). Use `triggers:` with `trigger: numeric_state` format per 10-RESEARCH.md patterns. Each automation must have: alias, id, trigger with entity_id + threshold + for (from delay_hours), and action with notify.mobile_app_REPLACEME placeholder. Group by bed (Bed A rules, then Bed B rules).

6. **Find Your Notification Service** -- Prominent callout explaining that notify.mobile_app_REPLACEME must be replaced with the user's actual service name. Step-by-step: Developer Tools > Actions > type "notify" > find mobile_app_<device_name>. Explain naming convention (phone name, lowercased, underscores). Include both iOS and Android paths to find device name.

**Style guidelines:**
- Use markdown headers (## for steps, ### for sub-sections)
- Every YAML block is a fenced code block with yaml language tag
- Include a "What you should see" note after each configuration step
- Use callout format (> **Note:** ...) for warnings and tips
- Keep instructions direct and action-oriented ("Open configuration.yaml", "Paste the following", "Restart Home Assistant")
- Use the bed names consistently: "Bed A (Family Bed)" and "Bed B (His Bed)"
  </action>
  <verify>
    <automated>grep -c "^## Step" docs/ha-setup-guide.md | grep -q "[6-8]" && grep -c "notify.mobile_app_REPLACEME" docs/ha-setup-guide.md | grep -q "[2-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Steps 1-6 exist with complete YAML blocks for sensors, customize, plant monitors, and all 10 automations. REPLACEME placeholder appears in every automation action.</done>
</task>

<task type="auto">
  <name>Task 2: Add dashboard YAML and verification checklist (Steps 7-8)</name>
  <files>docs/ha-setup-guide.md</files>
  <action>
Append Steps 7-8 to docs/ha-setup-guide.md:

7. **Lovelace Dashboard** -- Per-bed dashboard cards using the UI card editor (NOT yaml mode, which is deprecated in HA 2026.8). Instructions: "Edit dashboard > Add card > Manual > Show code editor > Paste YAML". Two vertical-stack cards:

   Bed A vertical-stack with:
   - Gauge card for moisture (min 0, max 100, needle, severity: green 40, yellow 25, red 0)
   - Gauge card for temperature (min -5, max 40, needle, severity: green 4, yellow 0, red -5)
   - Entity card for plant.haven_bed_a health status

   Bed B vertical-stack (same pattern, bed_b entities, adjust moisture max severity to 50 per plants.json thresholds)

   Source: 10-RESEARCH.md Lovelace Dashboard Cards section. Use the exact YAML from research, adjusting only if needed for Bed B thresholds.

   Include note: "Add each vertical-stack as a separate card. You should see gauge needles once sensors are reporting real data."

8. **Verification Checklist** -- A numbered checklist the father walks through after completing all steps:
   - [ ] Developer Tools > States shows sensor.haven_bed_a_moisture (and all 3 others)
   - [ ] Plant entities plant.haven_bed_a and plant.haven_bed_b appear in States
   - [ ] Automations list shows all 10 Haven automations (Settings > Automations)
   - [ ] Dashboard shows gauge cards for both beds (values will be 0 until sensors installed)
   - [ ] Test notification: Developer Tools > Actions > notify.mobile_app_<your_device> > call with test message
   - [ ] No errors in Settings > System > Logs after restart

Also add a brief **Quick Reference** section at the very end listing all entity names created by this guide (4 sensors, 2 plant monitors, 10 automations) as a flat table for easy lookup.
  </action>
  <verify>
    <automated>grep -c "vertical-stack" docs/ha-setup-guide.md | grep -q "[2-9]" && grep -c "Verification" docs/ha-setup-guide.md | grep -q "[1-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Dashboard YAML covers both beds with gauge + entity cards. Verification checklist covers all 16 entities. Quick reference table lists every entity name. Document is complete and self-contained.</done>
</task>

</tasks>

<verification>
- docs/ha-setup-guide.md exists with 8 numbered steps
- All YAML blocks use modern HA syntax (action: not service:, template: not platform: template, triggers: not trigger:)
- Every automation contains notify.mobile_app_REPLACEME placeholder
- Dashboard YAML uses UI card editor flow, not deprecated yaml mode
- Both beds (A and B) are covered in every section
- Step 6 clearly explains how to find the actual notification service name
</verification>

<success_criteria>
A father with Home Assistant running can follow the 8 steps sequentially, pasting YAML blocks into his configuration files, and end up with: 4 template sensors, 2 plant monitors, 10 alert automations, and a 2-bed dashboard with moisture/temperature gauges. The notification placeholder is prominently documented as requiring replacement.
</success_criteria>

<output>
After completion, create `.planning/phases/10-home-assistant-setup/10-01-SUMMARY.md`
</output>
