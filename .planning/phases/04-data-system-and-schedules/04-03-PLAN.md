---
phase: 04-data-system-and-schedules
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - data/ha/sensors.json
  - data/ha/plants.json
  - data/ha/customize.json
  - data/ha/alert-rules.json
  - data/sensors/sensor-recommendations.md
autonomous: true
requirements:
  - DATA-04
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "Home Assistant entity schemas use haven_ prefix with per-bed sensor entities for all 5 beds"
    - "Plant Monitor configurations define moisture and temperature thresholds per bed using the most restrictive crop thresholds"
    - "Alert rules include bilingual messages (da + en) with trigger types and delay hours"
    - "Sensor recommendations document 2-3 Zigbee options and 1 LoRaWAN fallback with DKK prices and HA compatibility"
    - "A clear primary 'buy this one' recommendation is stated"
  artifacts:
    - path: "data/ha/sensors.json"
      provides: "Template sensor definitions for per-bed Zigbee sensors"
      contains: "haven_bed_a_moisture"
    - path: "data/ha/plants.json"
      provides: "Plant Monitor configurations with per-bed thresholds"
      contains: "min_moisture"
    - path: "data/ha/customize.json"
      provides: "Entity customization with friendly names and icons"
      contains: "friendly_name"
    - path: "data/ha/alert-rules.json"
      provides: "Automation-ready alert rule definitions"
      contains: "delay_hours"
    - path: "data/sensors/sensor-recommendations.md"
      provides: "Hardware buying guide with prices and compatibility"
      contains: "Haozee"
  key_links:
    - from: "data/ha/plants.json"
      to: "data/ha/sensors.json"
      via: "sensor entity ID references"
      pattern: "sensor\\.haven_bed_[a-e]_moisture"
    - from: "data/ha/sensors.json"
      to: "data/crops/*.json"
      via: "threshold values derived from crop water_needs"
      pattern: "haven_bed"
    - from: "data/ha/alert-rules.json"
      to: "data/ha/sensors.json"
      via: "entity references in trigger rules"
      pattern: "sensor\\.haven_bed"
---

<objective>
Create Home Assistant integration schemas and sensor hardware recommendations.

Purpose: Establish the HA entity layer that maps bed sensors to crop thresholds, ready for v2 IoT expansion. The sensor recommendations give the user a clear buying guide. JSON is the source of truth; HA YAML would be derived from these files.

Output: `data/ha/` (4 JSON files), `data/sensors/sensor-recommendations.md`
</objective>

<execution_context>
@/Users/albair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-data-system-and-schedules/04-CONTEXT.md
@.planning/phases/04-data-system-and-schedules/04-RESEARCH.md
@.planning/phases/04-data-system-and-schedules/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Home Assistant JSON schemas</name>
  <files>data/ha/sensors.json, data/ha/plants.json, data/ha/customize.json, data/ha/alert-rules.json</files>
  <action>
Create `data/ha/` directory.

Create `data/ha/sensors.json` -- template sensor definitions for 5 beds:
- Per-bed sensors following haven_ prefix: `haven_bed_{a-e}_moisture`, `haven_bed_{a-e}_temperature`
- 10 sensor entries total (2 per bed: moisture + temperature)
- Each entry: unique_id, name, unit_of_measurement, device_class, state_template
- state_template references the expected Zigbee2MQTT entity (placeholder: `sensor.zigbee_soil_sensor_bed_{letter}_{measurement}`)
- Include a `yaml_output` field showing the exact HA template sensor YAML this JSON would generate (per research code example)

Create `data/ha/plants.json` -- Plant Monitor configurations for 5 beds:
- One Plant Monitor entity per BED (not per crop) as decided in CONTEXT.md
- For each bed, read the crop files from Plan 01 to find the most restrictive moisture/temperature thresholds:
  - min_moisture: highest min among bed's crops (safest -- alerts before any crop is too dry)
  - max_moisture: lowest max among bed's crops (safest -- alerts before any crop is too wet)
  - min_temperature: highest min_temp among bed's crops
- Reference sensor entity IDs from sensors.json
- Include `yaml_output` showing the exact Plant Monitor YAML

Create `data/ha/customize.json` -- entity customization:
- Friendly names for all 10 sensors: "Bed A (His Bed) - Soil Moisture", etc.
- Icons: mdi:water-percent for moisture, mdi:thermometer for temperature
- Include `yaml_output` showing customize.yaml

Create `data/ha/alert-rules.json` -- automation-ready alert rules:
- Per-bed alerts for: moisture_below_min, moisture_above_max, temperature_below_frost
- Each rule: id, trigger type, entity, threshold (from plants.json), delay_hours (6 for moisture, 0 for frost), message (en + da bilingual), notification_target
- Bilingual messages using i18n keys per CONTEXT.md locked decision
- These are DEFINITIONS for v2 automation, not live HA automations
- Include 15 alert rules total (3 per bed: dry, wet, frost)

All entity IDs must be consistent across the 4 files. Cross-reference crop threshold data from Plan 01 crop JSON files.
  </action>
  <verify>
    <automated>for f in data/ha/sensors.json data/ha/plants.json data/ha/customize.json data/ha/alert-rules.json; do python3 -m json.tool "$f" > /dev/null || echo "INVALID: $f"; done && python3 -c "import json; s=json.load(open('data/ha/sensors.json')); print('Sensor entries:', len(s.get('sensors', s.get('template', [])))); p=json.load(open('data/ha/plants.json')); print('Plant entries:', len(p.get('plants', []))); a=json.load(open('data/ha/alert-rules.json')); print('Alert rules:', len(a.get('rules', [])))" && echo "Valid"</automated>
  </verify>
  <done>4 HA JSON files exist. sensors.json has 10 template sensor entries (5 beds x 2 measurements). plants.json has 5 Plant Monitor configs with most-restrictive thresholds. customize.json has friendly names and icons. alert-rules.json has 15 bilingual alert rules. Entity IDs are consistent across all files.</done>
</task>

<task type="auto">
  <name>Task 2: Create sensor hardware recommendations document</name>
  <files>data/sensors/sensor-recommendations.md</files>
  <action>
Create `data/sensors/` directory.

Create `data/sensors/sensor-recommendations.md` structured as a buying guide with:

**Primary Recommendation (BUY THIS ONE):**
- Haozee Tuya Zigbee Soil Tester (TS0601_soil)
- Zigbee 3.0, soil moisture + temperature, IP66/IP67
- Battery: 2x AA, ~1 year
- Zigbee2MQTT: supported as TS0601_soil
- Price: ~150 DKK per unit (~750 DKK for 5 beds)
- Why: Community sensor shootout winner -- best range, minimal unit variance, stable long-term
- Where to buy: AliExpress search term, note IP66+ required for outdoor

**Alternative 1: ThirdReality 3RSM0147Z**
- Zigbee 3.0, IP67, 1x AA up to 3 years
- ~140 DKK per unit
- Tradeoff: narrow moisture range (50-70%), bulky, reports "humidity" requiring Z2M workaround

**Alternative 2 (LoRaWAN Fallback): Dragino LSE01**
- LoRaWAN, moisture + temperature + EC
- ~500-650 DKK per unit (plus gateway cost)
- When to use: ONLY if Zigbee range is insufficient from house to garden

**NOT Recommended: Tuya GXM-01**
- Documented issues: stuck at 100%/94%, rapid battery drain, manufacturer unresponsive

**Zigbee Range Extension:**
- IKEA TRETAKT smart plug (~100 DKK) in weatherproof outdoor outlet box
- Test range first -- 10-15m through one wall from coordinator to garden may work without repeater

**Budget Summary Table:**
| Item | Qty | Unit (DKK) | Total (DKK) |
| Haozee sensor | 5 | ~150 | ~750 |
| AA batteries | 10 | ~5 | ~50 |
| IKEA TRETAKT (optional) | 1 | ~100 | ~100 |
| Total | | | ~800-900 |

Fits within 500-1000 DKK budget target.

**Setup Notes:**
- Requires Zigbee coordinator (e.g., Sonoff ZBDongle-P) already in HA
- Requires Zigbee2MQTT addon installed
- Sensor calibration: may need TS0601_soil specific calibration offset in Z2M config
  </action>
  <verify>
    <automated>test -f data/sensors/sensor-recommendations.md && grep -q "Haozee" data/sensors/sensor-recommendations.md && grep -q "DKK" data/sensors/sensor-recommendations.md && grep -q "LoRaWAN" data/sensors/sensor-recommendations.md && echo "Valid"</automated>
  </verify>
  <done>sensor-recommendations.md exists with: primary Haozee recommendation clearly marked "buy this one", ThirdReality alternative, Dragino LoRaWAN fallback, GXM-01 warning, IKEA repeater option, budget table within 500-1000 DKK, and setup notes.</done>
</task>

</tasks>

<verification>
- All 4 HA JSON files are valid: `for f in data/ha/*.json; do python3 -m json.tool "$f" > /dev/null || echo "FAIL: $f"; done`
- Entity naming consistency: `grep -o 'haven_bed_[a-e]_[a-z]*' data/ha/sensors.json | sort -u` shows 10 unique entities
- Plant Monitor has 5 beds: `python3 -c "import json; print(len(json.load(open('data/ha/plants.json'))['plants']))"`
- Alert rules are bilingual: `grep -c '"da"' data/ha/alert-rules.json` shows at least 15 occurrences
- Sensor doc has primary recommendation: `grep -c "Haozee" data/sensors/sensor-recommendations.md`
</verification>

<success_criteria>
- DATA-04: sensors.json follows haven_ prefix with per-bed entities for all 5 beds
- DATA-05: plants.json has Plant Monitor configs with moisture/temperature thresholds derived from crop data
- DATA-06: sensor-recommendations.md documents 2-3 Zigbee + 1 LoRaWAN with specific models, DKK prices, and HA compatibility. Clear primary recommendation stated.
</success_criteria>

<output>
After completion, create `.planning/phases/04-data-system-and-schedules/04-03-SUMMARY.md`
</output>
