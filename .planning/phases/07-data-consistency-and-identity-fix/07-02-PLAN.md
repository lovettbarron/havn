---
phase: 07-data-consistency-and-identity-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - data/beds/bed-b.json
  - data/beds/bed-e.json
  - data/ha/alert-rules.json
  - data/crops/dill.json
  - data/crops/radish.json
  - data/crops/cherry-tomato-tumbling-tom.json
  - data/schedules/w19.json
  - data/schedules/w25.json
  - data/schedules/w30.json
autonomous: true
requirements:
  - DFIX-03
  - DFIX-04
  - DFIX-06
  - DFIX-07
  - DFIX-08

must_haves:
  truths:
    - "Bed JSON feature arrays correctly reflect reservoir and trellis assignments per Phase 2+ state"
    - "HA alert messages reference the correct crops for each bed (no Tumbling Tom in Bed E alerts)"
    - "A dill.json crop file exists with all required schema fields"
    - "Radish crop JSON documents why Bed E is excluded from its beds array"
    - "No weekly schedule sowing tasks reference stopped succession slots after Phase 3 transition"
  artifacts:
    - path: "data/crops/dill.json"
      provides: "Dill crop data file"
      contains: "Anethum graveolens"
    - path: "data/beds/bed-e.json"
      provides: "Corrected Bed E without Tumbling Tom"
    - path: "data/ha/alert-rules.json"
      provides: "Corrected alert messages for Bed E"
    - path: "data/crops/radish.json"
      provides: "Documented Bed E exclusion"
    - path: "data/schedules/w25.json"
      provides: "Removed stale Bed E lettuce sowing"
    - path: "data/schedules/w30.json"
      provides: "Removed stale Bed E lettuce sowing"
  key_links:
    - from: "data/beds/bed-e.json"
      to: "docs/planting-grid-bed-e.md"
      via: "Crop assignments match grid"
      pattern: "broccoli.*bush-bean"
    - from: "data/ha/alert-rules.json"
      to: "data/beds/bed-e.json"
      via: "Alert crops match bed crops"
      pattern: "bed_e.*broccoli.*bean"
    - from: "data/crops/dill.json"
      to: "data/beds/bed-c.json, data/beds/bed-e.json"
      via: "beds array matches grid assignments"
      pattern: "bed-c.*bed-e"
---

<objective>
Fix JSON data files: correct bed features and crop assignments, fix HA alert messages, create missing dill.json, document radish exclusion, and remove stale weekly schedule entries.

Purpose: Bed JSON, HA alerts, crop data, and weekly schedules must all agree with the planting grid source of truth. Incorrect data means wrong HA notifications, wrong sowing reminders, and a missing crop file.

Output: 9 corrected/created JSON files aligned with Phase 2+ planting grids.
</objective>

<execution_context>
@/Users/albair/.claude/get-shit-done/workflows/execute-plan.md
@/Users/albair/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-data-consistency-and-identity-fix/07-RESEARCH.md

Source of truth:
@docs/planting-grid-bed-c.md
@docs/planting-grid-bed-e.md

Templates:
@data/schemas/crop.schema.json
@data/crops/basil.json

<interfaces>
<!-- From 07-RESEARCH.md: -->

<!-- Bed B (west): features should include "self-watering-reservoir" IF user decides to keep it active. Default: leave empty, add description noting hardware exists but is unused. -->
<!-- Bed C (center): features = ["a-frame-trellis", "self-watering-reservoir"] (already correct) -->
<!-- Bed E (terrace north): remove cherry-tomato-tumbling-tom crop entry. Actual crops: broccoli, bush beans, dill, nasturtium, calendula -->

<!-- HA alert rules for Bed E: -->
<!-- havn_bed_e_moisture_below_min: change "broccoli and tomatoes" to "broccoli and beans" -->
<!-- havn_bed_e_temperature_below_frost: change "beans and tomatoes" to "beans" (broccoli is frost-tolerant) -->
<!-- Both en AND da translations must be updated -->

<!-- Dill crop data: -->
<!-- id: "dill", type: "herb", difficulty: "cant_fail" -->
<!-- beds: ["bed-c", "bed-e"] -->
<!-- name.da: "Dild", name.botanical: "Anethum graveolens" -->

<!-- Radish: beds array ["bed-a", "bed-c", "bed-d"] is CORRECT for Phase 3 state. Add a note documenting Phase 3 exclusion from Bed E. -->

<!-- Schedule fixes: -->
<!-- w25.json line 81: remove "Sow fourth succession lettuce in Bed E" -->
<!-- w30.json line 85: remove "Sow sixth (final) succession lettuce in Bed E" -->
<!-- w19.json line 69: add note that this is the last valid Bed E lettuce sowing before Phase 3 transition -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bed JSON, HA alerts, and crop data files</name>
  <files>data/beds/bed-b.json, data/beds/bed-e.json, data/ha/alert-rules.json, data/crops/dill.json, data/crops/radish.json, data/crops/cherry-tomato-tumbling-tom.json</files>
  <action>
Read each file before modifying. Apply corrections per the research findings:

**data/beds/bed-b.json:**
- Add a "notes" or "description" field (if the schema allows, or use an existing description field): "Reservoir hardware physically installed (was Bed 1/Tomato in Phase 1) but not actively used -- raspberries prefer free-draining soil."
- Leave features array as `[]` (default: reservoir not active).

**data/beds/bed-e.json:**
- Remove the `cherry-tomato-tumbling-tom` crop entry from the crops/assignments array (around lines 52-58). Bed E crops per grid: broccoli-calabrese, bush-bean-provider, dill, nasturtium, calendula.

**data/crops/cherry-tomato-tumbling-tom.json:**
- Read the file. Update its `beds` array: remove "bed-e" if present. If Tumbling Tom is pot-only, set beds to `[]` and add a note: "Backup pot overflow from Bed C; not assigned to any raised bed."

**data/ha/alert-rules.json:**
- Find rule `havn_bed_e_moisture_below_min`: Change message from "broccoli and tomatoes need water" to "broccoli and beans need water". Update BOTH `en` and `da` fields. Danish: change "tomater" to "bonner" (beans).
- Find rule `havn_bed_e_temperature_below_frost`: Change "beans and tomatoes are frost-sensitive" to "beans are frost-sensitive" (broccoli tolerates light frost). Update BOTH `en` and `da` fields. Danish: remove "tomater" reference.

**data/crops/dill.json (NEW FILE):**
- Create using basil.json as structural template and crop.schema.json for required fields.
- Key values:
  - id: "dill"
  - name: { common: "Dill", botanical: "Anethum graveolens", da: "Dild", shortcode: "DIL" }
  - type: "herb"
  - difficulty: { tier: "cant_fail", reason: "Self-sows readily, grows fast, minimal care needed" }
  - beds: ["bed-c", "bed-e"]
  - sowing: { method: "seed", outdoor_sow_week: 18, succession: false }
  - companion_plants: { good: ["cucumber", "broccoli", "lettuce"], bad: ["carrot"] }
  - engagement: child actions for each growth stage (seed sowing, watching feathery leaves, picking for cooking, watching flowers attract hoverflies)
  - alerts: standard herb alerts (moisture, frost)
  - Fill all other required schema fields with appropriate dill-specific data. Research reference: dill matures in 40-60 days, prefers full sun, moderate water, self-sows if flowers go to seed.

**data/crops/radish.json:**
- Read the file. The beds array `["bed-a", "bed-c", "bed-d"]` is CORRECT for Phase 3 state. Add a note field or comment in an appropriate location: "Bed E had radish succession slot S1 in Phase 2, but it was stopped at W20 when warm-season crops (broccoli, beans) took the space. Excluded from beds array per Phase 3 final state."

After all edits, validate every modified JSON file: `python3 -m json.tool <file> > /dev/null`
  </action>
  <verify>
    <automated>cd /Users/albair/src/havn && python3 -m json.tool data/crops/dill.json > /dev/null && echo "dill OK" && python3 -m json.tool data/beds/bed-e.json > /dev/null && echo "bed-e OK" && python3 -m json.tool data/ha/alert-rules.json > /dev/null && echo "alerts OK" && python3 -m json.tool data/crops/radish.json > /dev/null && echo "radish OK" && python3 -m json.tool data/beds/bed-b.json > /dev/null && echo "bed-b OK" && grep -i "tomato\|tomat" data/ha/alert-rules.json | grep -i "bed_e" | wc -l</automated>
  </verify>
  <done>All JSON files pass validation. dill.json exists with all required schema fields. Bed E has no Tumbling Tom crop entry. HA Bed E alerts reference "broccoli and beans" (not tomatoes). Radish beds array is unchanged but has a Phase 3 exclusion note. Bed B has a note about unused reservoir hardware. The final grep for "tomato" in Bed E alert rules returns 0 matches.</done>
</task>

<task type="auto">
  <name>Task 2: Audit and fix weekly schedule sowing tasks</name>
  <files>data/schedules/w19.json, data/schedules/w25.json, data/schedules/w30.json</files>
  <action>
Read each schedule file. Apply corrections per the research:

**data/schedules/w25.json:**
- Find and REMOVE the task "Sow fourth succession lettuce in Bed E" (around line 81). Bed E is fully transitioned to Phase 3 warm-season crops by W21. No space for lettuce.

**data/schedules/w30.json:**
- Find and REMOVE the task "Sow sixth (final) succession lettuce in Bed E" (around line 85). Same reason -- Bed E has no lettuce space after Phase 3 transition.

**data/schedules/w19.json:**
- Find the "Sow second succession lettuce in Bed E" task (around line 69). This entry falls in the Phase 2 window (before W21 transition), so it MAY be valid. Add a note to the task: "Last valid Bed E lettuce sowing -- Bed E transitions to warm-season crops from W20-W21." Do NOT remove this entry.

After edits, validate each modified JSON file: `python3 -m json.tool <file> > /dev/null`
  </action>
  <verify>
    <automated>cd /Users/albair/src/havn && for f in data/schedules/w19.json data/schedules/w25.json data/schedules/w30.json; do python3 -m json.tool "$f" > /dev/null && echo "$f OK" || echo "$f INVALID"; done && echo "---" && grep -rn "lettuce.*Bed E" data/schedules/w2[5-9].json data/schedules/w3*.json | wc -l</automated>
  </verify>
  <done>w25.json and w30.json have no Bed E lettuce sowing tasks. w19.json retains its entry with a "last valid" note. All three files pass JSON validation. Grep for "lettuce.*Bed E" in W25-W39 schedule files returns 0 matches.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `for f in data/crops/*.json data/beds/*.json data/ha/*.json; do python3 -m json.tool "$f" > /dev/null || echo "INVALID: $f"; done` -- all files valid
2. `python3 -m json.tool data/crops/dill.json > /dev/null` -- dill.json exists and is valid
3. `grep -i "tomato\|tomat" data/ha/alert-rules.json | grep -i "bed_e"` -- 0 matches
4. `grep -rn "lettuce.*Bed E" data/schedules/w2[5-9].json data/schedules/w3*.json` -- 0 matches
5. `python3 -c "import json; d=json.load(open('data/beds/bed-e.json')); print([c for c in d.get('crops',d.get('assignments',[])) if 'tomato' in str(c).lower()])"` -- empty list
</verification>

<success_criteria>
- dill.json exists with all required crop schema fields, beds=["bed-c","bed-e"]
- Bed E JSON has no Tumbling Tom crop entry
- HA Bed E alert messages say "broccoli and beans" not "broccoli and tomatoes" (both en and da)
- Radish beds array unchanged with Phase 3 exclusion note
- Bed B JSON notes unused reservoir hardware
- Cherry-tomato-tumbling-tom.json beds array does not include bed-e
- W25 and W30 schedules have no Bed E lettuce sowing tasks
- W19 schedule has a "last valid" note on its Bed E lettuce task
- All JSON files pass python3 -m json.tool validation
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-consistency-and-identity-fix/07-02-SUMMARY.md`
</output>
